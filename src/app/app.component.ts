import { Component } from '@angular/core';

@Component({
    selector: 'app-root',
    templateUrl: './app.component.html',
    styleUrls: ['./app.component.css']
})
export class AppComponent {
    // recipe = true;
    // shopping= false;

    // toRecipes(info: string) {
    //     if (info) {
    //         this.recipe = true;
    //         this.shopping = false;
    //     }
    // }

    // toShopping(info: string) {
    //     if (info) {
    //         this.shopping = true;
    //         this.recipe = false;
    //     }
    // }

    // html
    // (recipes)="toRecipes($event)"
    // (shopping)="toShopping($event)"
}
